# 改动日志（验证后登记）

本文件用于记录在**你本地或联调验证通过**后、值得留档的变更：**日期**、**改动原因**、**具体改动**写清楚，便于以后回顾与交接。

- **如何追加**：你验证某次改动 OK 后，在对话里说明「把 xxx 记进改动日志」，维护者按你的描述在下方 **「记录」** 区**倒序追加**（最新在上），或你自行编辑本文件。
- **日期**：以**验证通过当日**为准，格式 `YYYY-MM-DD`；若跨日合并发布，可写区间或主验证日。追加或改正日期时请对照**本机当前日期**（勿使用尚未到来的日历日）。
- **原因**：业务背景、要解决的问题、关联现象（一两段即可）。
- **改动内容**：涉及的路径、接口、配置项、行为变化，可用列表；必要时写「破坏性变更」「需清数据」等提示。

---

## 记录

<!-- 新条目请加在本注释下方；建议最新日期在最上 -->

### 2026-04-28

#### 1. 分支 `nanxing-db`：供应方图数据库 HTTP 检索 + 本地大模型作答（可选 `RAG_BACKEND`）

**原因：** 在部分部署中不再使用本地 Embedding + Chroma 做检索，改为调用供应方提供的图检索 HTTP 接口（`query` / `domain` / `search_type`），将返回的三元组、摘要、文本块作为上下文，由现有火山兼容 **`chat.completions`** 生成回答；开发与文档约定后端依赖在 **`backend/venv`** 中安装与运行测试。

**分支说明：** 以下改动在 Git 分支 **`nanxing-db`** 上开发与自测；通过环境变量 **`RAG_BACKEND=external_graph`** 启用图方案，**默认仍为 `chromadb`**，未改 `.env` 时行为与原先本地向量 RAG 一致。

**改动内容：**
- **`backend/config.py`**：新增 **`RAG_BACKEND`**、**`EXTERNAL_GRAPH_API_URL`**、**`EXTERNAL_GRAPH_DOMAIN`**、**`EXTERNAL_GRAPH_SEARCH_TYPE`**、**`EXTERNAL_GRAPH_TIMEOUT`**（默认 URL / 域与供应方约定一致，可按环境覆盖）。
- **`backend/core/external_graph.py`**（新）：**`httpx`** POST 图接口；解析 **`success` + `message.triplet|summary|chunk`**；字符串或常见字典三元组格式化为可读行；**`config.settings`** 运行时读取以便单测 patch；组装 **`sources`**（`graph-triplet-*` 等、`document_title` 含「图数据库·…」）。
- **`backend/core/rag.py`**：`query()` 在 **`external_graph`** 时走 **`_query_external_graph`**；图无命中 / 未配 LLM 等分支与 **`_generate_answer_graph`** 专用 system 提示；成功时 **`answer_mode`** 为 **`external_graph`**。
- **`backend/main.py`**：图模式下跳过本地 **`EmbeddingGenerator`** 预热。
- **`backend/api/query.py`**：**`QueryResponse` / `ChatResponse`** 的 **`answer_mode`** 说明含 **`external_graph`**。
- **`backend/tests/core/test_external_graph.py`**（新）：图客户端与结构化三元组格式化单测。
- **`CLAUDE.md`**：明确后端 **`pip` / `pytest`** 使用 **`backend/venv`**，勿用系统全局 Python。
- **`doc/外部图数据库RAG实现说明.md`**（新）：架构、配置、供应方请求形态、关键代码引用与测试命令。
- **`frontend/src/types/index.ts`**：**`AnswerMode`** 增加 **`external_graph`**。
- **`frontend/src/pages/ChatInterface.tsx`**：**`formatAssistantSourceLine`** 对 **`external_graph`** 显示「来源：供应方图数据库检索结果…」；与 **`sources`** 区块「引用片段」一致便于区分来源。

**验证：** 用户确认前端提问与 Swagger **`POST /api/query`** 在合法 JSON 下行为正常；Swagger 手填 body 时需注意 **JSON 逗号**等语法（否则会 **422 json_invalid**）。**`POST /api/search`** 仍为本地向量检索，不受 **`RAG_BACKEND`** 影响。

---

### 2026-04-24

#### 1. 分支 `feature/doubao-embedding`：方舟嵌入（含多模态 vision 接入点）与相关说明

**原因：** 使用火山方舟豆包 **embedding** 与可选的**独立嵌入 API Key**；**doubao-embedding-vision** 等模型不能走标准 `/embeddings` 文本接口，需走 **`/embeddings/multimodal`**；解析返回体时兼容 `data` 为**单对象**与**数组**两种形态；配置项易与「读文档长度」混淆处加注释；内嵌图暂不实现单独记入待办文档。

**分支说明：** 以下改动在 Git 分支 **`feature/doubao-embedding`** 上开发与自测，**尚未合并 `main`**（以你仓库实际分支名为准）。

**改动内容：**
- **`backend/config.py`**：`EMBEDDING_BACKEND` / `EMBEDDING_MODEL` / `EMBEDDING_DIMENSION` / `CHROMADB_COLLECTION`；可选 **`VOLCENGINE_EMBEDDING_API_KEY`**；**`EMBEDDING_USE_MULTIMODAL_API`**（vision 等多模态嵌入走单独 API）；**`MAX_TOKENS`** 注释（标明为 **chat 补全长度**，非文档上下文）。
- **`backend/storage/vector_store.py`**：`EmbeddingGenerator` 在 `EMBEDDING_USE_MULTIMODAL_API` 时用 **`httpx`** 请求 **`{VOLCENGINE_BASE_URL}/embeddings/multimodal`**；**`_ark_embedding_data_rows`** 规范化 `data` 字段；**`chunk.metadata`** 可选 **`embedding_image_urls`** / **`image_urls`**（公网 URL）拼入多模态 `input`（当前流水线不自动填图）。
- **`backend/main.py`**：嵌入预热日志区分是否多模态路径。
- **`backend/requirements.txt`**：增加 **`httpx`**。
- **`backend/.env.example`**：嵌入与多模态、独立 Key 等说明。
- **`backend/tests/storage/test_embedding_volcengine.py`**：标准嵌入与多模态路径、`data` 单对象解析等单测。
- **`doc/后续工作-多模态嵌入与文档内嵌图.md`**：记录「暂只做文字、文档内粘贴图后续开发」及技术要点。

**验证：** 简单测试已通过（用户确认记入本日志）。切换嵌入维度/模型时仍需按说明**换新集合或清空 `vector_db`**，避免与旧向量混写。

---

### 2026-04-23

#### 1. 检索无命中统一走 LLM；移除预设回答与 `CHAT_ALLOW_GENERAL_WITHOUT_DOCS`

**原因：** 不希望知识库未命中时返回本地固定长文案；希望 **`/api/chat`** 与 **`/api/query`** 在无命中时均由已配置的大模型处理（未配 Key/模型时保留简短配置提示）。

**改动内容：**
- `backend/core/rag.py`：删除 **`_no_context_answer`**；`retrieved` 为空且已配 **`VOLCENGINE_API_KEY`** 与 **`LLM_MODEL`** 时调用 **`_generate_answer_without_context`**；移除 **`allow_general_llm_when_empty`** 参数。
- `backend/api/query.py`：两处 `query()` 调用不再传该布尔参数。
- `backend/config.py`、**`backend/.env.example`**：移除 **`CHAT_ALLOW_GENERAL_WITHOUT_DOCS`**。
- `backend/tests/core/test_rag_no_retrieval.py`、`backend/tests/api/test_query.py`：同步断言。
- 文档：`doc/测试方法.md`、`doc/项目改动总结.md`、`doc/后续工作-嵌入本地化与无检索闲聊.md` 与上述行为对齐。

**验证：** 待你本地确认（无命中时回答为 LLM 生成、`sources` 为空；未配 Key 时为配置提示）。

---

### 2026-04-22

#### 1. 无检索命中时：通用 LLM 的 system 提示带上索引统计（避免「你没上传」式误导）

**原因：** Chat 在向量检索**零命中**时仍走 `_generate_answer_without_context`。旧 system 一味强调「去 Documents 上传、处理完成后再问」，模型容易把「本轮没匹配到片段」说成「没有可查询文档 / 仍未上传处理」，与 Dashboard 已有 chunk、Documents 已为 **completed** 的事实矛盾。

**改动内容：**
- `backend/core/rag.py`：新增 `_system_prompt_for_no_retrieval_match()`，读取 `vector_store.get_chunk_count()` 与 `document_store.count_by_status()` 中 **completed** 数量；**片段数为 0** 与 **大于 0** 分支说明不同；索引非空时明确要求模型**不要**断言用户未完成上传，并提示语义匹配、**SIMILARITY_THRESHOLD**、可看 Documents / Dashboard。
- `backend/api/query.py`：`RAGPipeline(..., document_store=main.document_store)`。
- `backend/tests/core/test_rag_no_retrieval.py`：为 mock 向量库补充 `get_chunk_count`；新增 `test_no_retrieval_system_prompt_when_index_non_empty`。

**验证：** 已验证（用户确认记入本日志）。

---

#### 2. 上传文档的 `document_id` 与向量库 chunk 一致（删除能清掉向量）

**原因：** 上传接口先创建并保存 `Document`（id=A），`DocumentProcessor.process_document` 内部又 `new` 了另一个 `Document`（id=B），分块写入 Chroma 时 `metadata.document_id` 为 **B**，而列表与删除接口使用的是 **A**。结果是：`DELETE` 无法按 A 删除向量、Dashboard **chunks 总数不随删文档减少**、已删文档仍可能被语义检索命中。

**改动内容：**
- `backend/core/processor.py`：`process_document(..., document=None)`，若传入 API 已创建的 `Document`，在同一实例上更新元数据与状态，且 `_create_chunks(..., document.id)` 使用 **A**。
- `backend/api/documents.py`：上传时传入 `document=document`；处理未 `COMPLETED` 时返回 500；`except` 中先 `raise HTTPException` 再捕获其它异常。
- `backend/tests/core/test_processor.py`：新增 `test_process_document_reuses_api_document_id`。
- `backend/tests/api/test_documents.py`：上传相关 mock 改为 `side_effect`，对传入的 `document` 置 `COMPLETED` 并返回同一引用。

**验证：** 已验证（删除文档后 chunk 数减少、不再检索到已删内容；历史脏数据需自行清空 `vector_db` 后重传）。

---

#### 3. Chat 对话在离开页面后仍保留（浏览器本地）

**原因：** 聊天记录仅存于 `ChatInterface` 的 `useState`，路由切到 Documents / Dashboard 后组件卸载，**对话丢失**。

**改动内容：**
- `frontend/src/pages/ChatInterface.tsx`：使用 `localStorage` 键 `documents-analysis-chat-messages-v1`；初次挂载从本地恢复；`messages` 变化时写入；确认清空对话时移除该项。

**验证：** 已验证（离开 Chat 再返回，对话仍在；清空垃圾桶后本地记录清除）。

---

#### 4. Documents 列表增加「页数」列；DOCX 补充 `metadata.page_count`

**原因：** PDF 处理结果里已有 `page_count`，但前端未展示；DOCX 未写入页数，无法在列表与 Word 页数对照，难以判断「版式页数是否被识别」。

**改动内容：**
- `frontend/src/pages/DocumentManager.tsx`：表格在 Status 与 Chunks 之间增加列 **「页数」**，展示 `metadata.page_count`；缺失时显示 **—**。
- `frontend/src/types/index.ts`：`Document.metadata` 增加可选字段 `page_count`。
- `backend/core/processor.py`：新增 `_read_docx_page_count()`，从 DOCX 包内 `docProps/app.xml` 读取 Word 扩展属性中的 `<Pages>`（部分导出文件可能无此项）；`_extract_docx` 中赋值 `metadata.page_count`。
- `backend/tests/core/test_processor.py`：为 `_read_docx_page_count` 增加单元测试。

**验证：** 待你本地确认（PDF/DOCX 各传一份，列表页数与 Word/PDF 页数一致；无 `<Pages>` 的 docx 仍为 —；历史已入库 DOCX 需重传后才有页数）。

---
